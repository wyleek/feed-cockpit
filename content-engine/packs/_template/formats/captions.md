# Format: platform captions

One natively-written caption per platform in `pack.md`. Never the same text pasted across platforms.

## Per-platform specs ({fill in for each platform in pack.md})

### {Platform}
- Length: {e.g., IG: 1-2 short paragraphs + line-broken}
- Source link handling: {in-caption / "link in bio" / first comment — platform punishes links?}
- Hashtags: {count + placement + style; niche > broad}
- CTA norms: {what works here}

## Universal rules
- Caption restates the receipt: source named in-text even when the link is elsewhere.
- First line must work standalone (it's the preview line everywhere).
- Caption adds something beyond the script (a detail that didn't fit, the caveat, the question to the comments) — never a transcript.
